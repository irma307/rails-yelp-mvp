<h1>All restaurants</h1>
<% @restaurants.each do |resto| %>
<p><%= link_to resto.name, restaurant_path(resto) %> <%= resto.category %> <%= link_to "edit", edit_restaurant_path(resto) %> | link_to "delete", restaurant_path(resto), method: :delete </p>
<% end %>
